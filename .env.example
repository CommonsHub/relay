# Admin API bearer token (generate with: openssl rand -hex 32)
RELAY_ADMIN_SECRET=change-me

# Comma-separated IPs allowed to call the admin API
# Should include the CommonsHub API server IP
ADMIN_ALLOWED_IPS=127.0.0.1

# strfry data directory (inside container)
STRFRY_DATA_DIR=/data
